/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["source.unsplash.com"], // 👈 permite usar imagens externas do Unsplash
  },
};

module.exports = nextConfig;
